{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "region": "asia-southeast1",
    "startCommand": "python manage.py collectstatic --noinput --verbosity 0 && python manage.py migrate --noinput && gunicorn --reload interintel.wsgi:application",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
